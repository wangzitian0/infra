name: CI Pipeline

on:
  pull_request:
    branches: [main]

jobs:
  test-basic:
    name: Basic Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "✅ Step 1: Basic inline job works"
      - run: echo "Files changed:" && git diff --name-only origin/main..HEAD || true
