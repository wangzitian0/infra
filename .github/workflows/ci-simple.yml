name: CI Pipeline Simple

on:
  pull_request:
    branches: [main]

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "✅ Check job works"
  
  summary:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "## ⚠️ CI 重构进行中"
          echo "Reusable workflows 遇到问题，临时使用简化版本"
