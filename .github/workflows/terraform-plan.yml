name: Terraform CI

on:
  pull_request:
    branches: [main]
    paths:
      - "1.bootstrap/**"
      - "2.platform/**"
      - "3.data/**"
      - "4.apps/**"
      - ".github/workflows/terraform-*.yml"

# Cancel in-progress runs for same PR
concurrency:
  group: terraform-${{ github.event.pull_request.number }}-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: true

env:
  TF_IN_AUTOMATION: "true"
  TF_INPUT: "false"

jobs:
  validate:
    name: CI Validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ============================================================
      # Setup Terraform
      # ============================================================
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.6

      # ============================================================
      # Static Analysis (all layers)
      # ============================================================
      - name: Format Check
        id: fmt
        run: terraform fmt -check -recursive -diff
        continue-on-error: true

      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: latest

      - name: TFLint (L1)
        id: lint_l1
        working-directory: 1.bootstrap
        run: tflint --init && tflint --recursive --minimum-failure-severity=error
        continue-on-error: true

      - name: TFLint (L2)
        id: lint_l2
        working-directory: 2.platform
        run: tflint --init && tflint --recursive --minimum-failure-severity=error
        continue-on-error: true

      - name: TFLint (L3)
        id: lint_l3
        working-directory: 3.data
        run: tflint --init && tflint --recursive --minimum-failure-severity=error
        continue-on-error: true

      - name: TFLint (L4)
        id: lint_l4
        working-directory: 4.apps
        run: tflint --init && tflint --recursive --minimum-failure-severity=error
        continue-on-error: true

      - name: Pre-flight Check (Helm URLs)
        run: |
          chmod +x 0.tools/preflight-check.sh
          0.tools/preflight-check.sh

      # ============================================================
      # Terraform Validation (syntax-only: no backend / no remote services)
      # ============================================================
      - name: Validate (L1)
        id: validate_l1
        working-directory: 1.bootstrap
        run: terraform init -backend=false -input=false -no-color && terraform validate -no-color
        continue-on-error: true

      - name: Validate (L2)
        id: validate_l2
        working-directory: 2.platform
        run: terraform init -backend=false -input=false -no-color && terraform validate -no-color
        continue-on-error: true

      - name: Validate (L3)
        id: validate_l3
        working-directory: 3.data
        run: terraform init -backend=false -input=false -no-color && terraform validate -no-color
        continue-on-error: true

      - name: Validate (L4)
        id: validate_l4
        working-directory: 4.apps
        run: terraform init -backend=false -input=false -no-color && terraform validate -no-color
        continue-on-error: true

      # ============================================================
      # infra-flash Comment (Per-commit, new comment for each commit)
      # ============================================================
      - name: Generate App Token
        if: always()
        id: app_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.ATLANTIS_GH_APP_ID }}
          private-key: ${{ secrets.ATLANTIS_GH_APP_KEY }}

      - name: Create infra-flash comment
        if: always()
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app_token.outputs.token }}
          script: |
            const fmt = '${{ steps.fmt.outcome }}' || 'skipped';
            const lint_l1 = '${{ steps.lint_l1.outcome }}' || 'skipped';
            const lint_l2 = '${{ steps.lint_l2.outcome }}' || 'skipped';
            const lint_l3 = '${{ steps.lint_l3.outcome }}' || 'skipped';
            const lint_l4 = '${{ steps.lint_l4.outcome }}' || 'skipped';
            const validate_l1 = '${{ steps.validate_l1.outcome }}' || 'skipped';
            const validate_l2 = '${{ steps.validate_l2.outcome }}' || 'skipped';
            const validate_l3 = '${{ steps.validate_l3.outcome }}' || 'skipped';
            const validate_l4 = '${{ steps.validate_l4.outcome }}' || 'skipped';
            
            const icon = (s) => s === 'success' ? '✅' : (s === 'skipped' ? '⏭️' : '❌');
            const allPass = [fmt, lint_l1, lint_l2, lint_l3, lint_l4, validate_l1, validate_l2, validate_l3, validate_l4]
              .every(s => s === 'success' || s === 'skipped');
            const sha = '${{ github.event.pull_request.head.sha }}'.substring(0, 7);
            const timestamp = new Date().toISOString().slice(11, 16) + ' UTC';
            
            const nextStep = allPass
              ? '⏳ **Atlantis autoplan** will run automatically. After plan is posted, review it then comment `atlantis apply`.'
              : '👉 **Next:** Fix errors locally, then `git push`';
            
            const fixCmd = '```bash\n# Fix locally:\nterraform fmt -recursive\nterraform validate\ngit push\n```';
            
            const body = [
              `<!-- infra-flash-commit:${sha} -->`,
              `## ⚡ Commit \`${sha}\``,
              '',
              `### CI Validate ${allPass ? '✅' : '❌'} | ${timestamp}`,
              '',
              '| Layer | Format | Lint | Validate |',
              '|:------|:------:|:----:|:--------:|',
              `| L1 Bootstrap | ${icon(fmt)} | ${icon(lint_l1)} | ${icon(validate_l1)} |`,
              `| L2 Platform | ${icon(fmt)} | ${icon(lint_l2)} | ${icon(validate_l2)} |`,
              `| L3 Data | ${icon(fmt)} | ${icon(lint_l3)} | ${icon(validate_l3)} |`,
              `| L4 Apps | ${icon(fmt)} | ${icon(lint_l4)} | ${icon(validate_l4)} |`,
              '',
              !allPass ? fixCmd : '',
              '',
              nextStep
            ].join('\n');
            
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: body
            });
            
            console.log(`Created new comment for commit ${sha}`);

      # ============================================================
      # Final Status (fail if any check failed)
      # ============================================================
      - name: Check Results
        run: |
          FAILED=0
          [[ "${{ steps.fmt.outcome }}" != "success" ]] && echo "❌ Format check failed" && FAILED=1
          [[ "${{ steps.lint_l1.outcome }}" != "success" ]] && echo "❌ L1 lint failed" && FAILED=1
          [[ "${{ steps.lint_l2.outcome }}" != "success" ]] && echo "❌ L2 lint failed" && FAILED=1
          [[ "${{ steps.lint_l3.outcome }}" != "success" ]] && echo "❌ L3 lint failed" && FAILED=1
          [[ "${{ steps.lint_l4.outcome }}" != "success" ]] && echo "❌ L4 lint failed" && FAILED=1
          [[ "${{ steps.validate_l1.outcome }}" != "success" ]] && echo "❌ L1 validate failed" && FAILED=1
          [[ "${{ steps.validate_l2.outcome }}" != "success" ]] && echo "❌ L2 validate failed" && FAILED=1
          [[ "${{ steps.validate_l3.outcome }}" != "success" ]] && echo "❌ L3 validate failed" && FAILED=1
          [[ "${{ steps.validate_l4.outcome }}" != "success" ]] && echo "❌ L4 validate failed" && FAILED=1
          
          if [[ $FAILED -eq 1 ]]; then
            echo "::error::CI validation failed"
            exit 1
          fi
          echo "✅ All CI checks passed"
