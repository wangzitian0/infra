name: Update infra-flash Comment

"on":
  issue_comment:
    types: [created]

jobs:
  update:
    # 只处理 Atlantis bot 在 PR 上的评论
    if: |
      github.event.issue.pull_request &&
      github.event.comment.user.login == 'infra-flash[bot]' &&
      (contains(github.event.comment.body, 'Ran Plan') || 
       contains(github.event.comment.body, 'Ran Apply'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write

    steps:
      - name: Generate App Token
        id: app_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.ATLANTIS_GH_APP_ID }}
          private-key: ${{ secrets.ATLANTIS_GH_APP_KEY }}

      - name: Get PR head SHA
        id: pr_sha
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app_token.outputs.token }}
          result-encoding: string
          script: |
            const pr = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.issue.number
            });
            return pr.data.head.sha.substring(0, 7);

      - name: Update infra-flash comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ steps.app_token.outputs.token }}
          script: |
            const prNumber = context.payload.issue.number;
            const atlantisBody = context.payload.comment.body;
            const atlantisCommentUrl = context.payload.comment.html_url;
            const timestamp = new Date().toISOString().slice(11, 16) + ' UTC';
            const currentSha = '${{ steps.pr_sha.outputs.result }}';
            const shaMatch = atlantisBody.match(/infra-flash-commit:([0-9a-f]{7,40})/i);
            const targetSha = (shaMatch ? shaMatch[1] : currentSha).substring(0, 7);
            
            // 判断是 plan 还是 apply
            const isPlan = atlantisBody.includes('Ran Plan');
            const isApply = atlantisBody.includes('Ran Apply');
            const isSuccess = !/(failed|error|panic:)/i.test(atlantisBody);
            
            const stage = isPlan ? 'Plan' : 'Apply';
            const icon = isSuccess ? '✅' : '❌';
            
            // 获取所有评论（分页），找触发命令的人类评论
            const comments = await github.paginate(github.rest.issues.listComments, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              per_page: 100
            });
            
            // 找到触发此操作的人类评论（在 Atlantis 评论之前，包含 atlantis plan/apply）
            const atlantisCommentTime = new Date(context.payload.comment.created_at);
            const triggerComment = comments
              .filter(c => c.user?.type !== 'Bot')
              .filter(c => new Date(c.created_at) < atlantisCommentTime)
              .filter(c => (c.body || '').toLowerCase().includes(`atlantis ${stage.toLowerCase()}`))
              .pop();  // 取最近的一条
            
            const triggerUrl = triggerComment ? triggerComment.html_url : null;
            
            // 找到当前操作对应的 infra-flash 评论：
            // - 优先：从 Atlantis 输出解析 commit（infra-flash-commit:xxxxxxx）并精确匹配
            // - 兜底：用时间对齐最近一次 CI 评论（避免执行期间 PR 又 push 导致 head SHA 漂移）
            const flashCommentByMarker = comments.find(c =>
              c.user.login === 'infra-flash[bot]' &&
              c.body.includes(`<!-- infra-flash-commit:${targetSha} -->`)
            );

            const anchorTime = atlantisCommentTime;
            const flashCommentByAnchorTime = comments
              .filter(c => c.user.login === 'infra-flash[bot]')
              .filter(c => c.body.includes('<!-- infra-flash-commit:'))
              .filter(c => new Date(c.created_at) <= anchorTime)
              .sort((a, b) => new Date(a.created_at) - new Date(b.created_at))
              .pop();

            const flashComment = flashCommentByMarker || flashCommentByAnchorTime;
            
            if (!flashComment) {
              console.log(`No infra-flash comment found (targetSha=${targetSha}, headSha=${currentSha})`);
              return;
            }
            
            // 确定下一步操作
            let nextStep = '';
            if (isPlan && isSuccess) {
              nextStep = '\n\n👉 **Next:** Review plan, then comment `atlantis apply`';
            } else if (isApply && isSuccess) {
              nextStep = '\n\n✅ **Ready to merge!**';
            } else if (!isSuccess) {
              nextStep = isPlan
                ? '\n\n👉 **Next:** Fix errors (push if needed). Atlantis will autoplan again, or comment `atlantis plan` to retry.'
                : '\n\n👉 **Next:** Fix errors, then comment `atlantis apply` (or rerun `atlantis plan`)';
            }
            
            // 移除旧的 next step 提示
            let updatedBody = flashComment.body
              .replace(/\n*👉 \*\*Next:\*\*.*/g, '')
              .replace(/\n*✅ \*\*Ready to merge!\*\*/g, '');
            
            // 构建链接
            const triggerLink = triggerUrl
              ? `[Triggered by](${triggerUrl})`
              : (isPlan ? 'Autoplan' : 'Manual trigger');
            const outputLink = `[View output](${atlantisCommentUrl})`;
            
            // 构建新的 Atlantis 状态块（注意：不要在 YAML 的 script block 里输出未缩进的纯文本行）
            const newSection = [
              '',
              '---',
              '',
              `### Atlantis ${stage} ${icon} | ${timestamp}`,
              '',
              `📎 ${triggerLink} → ${outputLink}${nextStep}`
            ].join('\n');
            
            // 移除旧的同类型状态（避免重复）
            const sectionRegex = new RegExp(`\\n---\\n+### Atlantis ${stage}[\\s\\S]*?(?=\\n---|$)`, 'g');
            updatedBody = updatedBody.replace(sectionRegex, '');
            
            // 追加新状态
            updatedBody = updatedBody.trim() + newSection;
            
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: flashComment.id,
              body: updatedBody
            });
            
            console.log(`Updated comment for commit ${targetSha} with ${stage} ${icon}`);
