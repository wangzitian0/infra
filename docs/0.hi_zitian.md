# 需要 Zitian 做的事情

## ✅ 已有资源

- ✅ VPS: HostHatch (cloud.hosthatch.com)
- ✅ Cloudflare 账号

## 🎯 配置步骤（全自托管，Terraform 管理，自托管 Infisical）

1) **Cloudflare API Token / Zone ID**  
   - Cloudflare → My Profile → API Tokens → Create Token（模板: Edit zone DNS）  
   - 记录 `CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ZONE_ID`

2) **VPS SSH 访问**  
   - 获取 VPS IP（HostHatch 控制面板）  
   - 如无密钥，生成并下发：  
   ```bash
   ssh-keygen -t ed25519 -C "zitian@truealpha"
   ssh-copy-id root@<vps-ip>
   ssh root@<vps-ip>
   ```

3) **安装基础依赖（一次性，单台 VPS）**  
   ```bash
   curl -fsSL https://get.docker.com | sh        # Docker
   curl -sSL https://dokploy.com/install.sh | sh # Dokploy + Traefik
   ```

4) **自托管 Infisical（容器）**  
   - 用 Dokploy/Compose 部署 Infisical（参考 iac_sop / compose/platform/infisical.yml）  
   - 创建 Project `truealpha`，环境：staging / test / prod  
   - 导入 `secrets/.env.example` 全部变量（含 SSH/Cloudflare/DB/应用等）  
   - 为每个环境创建 Machine Identity (MI)，记录 Client ID/Secret/Project ID

5) **GitHub Secrets（最小化）**  
   Settings → Secrets and variables → Actions 仅添加：  
   - `INFISICAL_CLIENT_ID`  
   - `INFISICAL_CLIENT_SECRET`  
   - `INFISICAL_PROJECT_ID`  
   > SSH/Cloudflare/DB/应用等全部留在自托管 Infisical。

6) **Terraform（从 staging 开始，管理 Cloudflare/DNS/WAF/基建）**  
   ```bash
   cd terraform/envs/staging   # 先做 staging，再 test/prod
   terraform init
   terraform plan
   terraform apply
   ```

7) **应用部署（全自动，无 UI）**  
   ```bash
   ./scripts/deploy/export-secrets.sh staging   # 从 Infisical 拉取全部变量
   ./scripts/deploy/deploy.sh staging           # 调用 Dokploy API/CLI 应用 compose
   ```

8) **验证**  
   ```bash
   dig x-staging.truealpha.club
   curl -I https://x-staging.truealpha.club
   curl https://api-x-staging.truealpha.club/graphql
   ```

## 📌 关键规则
- 域名（扁平）：test `x-test.*` + PR `x-test-*.*`；staging `x-staging.*`；prod `truealpha.*`
- GitHub Secrets 仅 MI 三元组；其他密钥均在自托管 Infisical。
- Dokploy 部署通过脚本/CI 调用 API/CLI，避免 UI 点选。

## 🗒️ 待办（按优先级）
- [ ] 完成 Cloudflare Token/Zone ID
- [ ] 完成 VPS SSH 配置
- [ ] 部署自托管 Infisical 并创建 MI
- [ ] 填写 GitHub Secrets（MI 三元组）
- [ ] 运行 Terraform（staging → test → prod）
- [ ] 部署 staging 应用并验证
- [ ] 部署 SigNoz / PostHog（自托管）
