# Automation Scripts

## 📍 你在这里

这个目录包含所有自动化脚本。

## 📂 目录结构

```
scripts/
├── README.md           → 本文件
├── deploy/             → 部署相关
│   ├── export-secrets.sh  → 从 Infisical 导出环境变量
│   └── deploy.sh          → 自动化部署脚本
├── setup/              → 初始化脚本（预留）
└── backup/             → 备份脚本（预留）
```

## 🚀 部署脚本

### export-secrets.sh

从 Infisical 导出环境变量到 `.env` 文件。

**用法**:
```bash
./scripts/deploy/export-secrets.sh dev
# 生成 .env.dev
```

### deploy.sh

自动化部署到指定环境。

**用法**:
```bash
./scripts/deploy/deploy.sh dev
./scripts/deploy/deploy.sh staging v1.2.3
```

**功能**:
- 导出环境变量（如果没有）
- 验证配置
- 拉取最新镜像
- 零停机部署
- 健康检查
- 生产环境二次确认

## ⚠️ 重要提示

- 所有 `.sh` 脚本已设置执行权限
- 生产环境部署需要手动确认
- 脚本会自动验证配置有效性

## 📚 更多文档

- [运维手册](../docs/runbooks/operations.md)
- [开发者指南](../docs/guides/developer-onboarding.md)
