# Backend config for staging environment
# Used by: Digger workflow
# Note: bucket and endpoints use placeholders - actual values from env vars in Digger workflow

bucket = "PLACEHOLDER_BUCKET"
key    = "k3s/staging.tfstate"
endpoints = { s3 = "https://PLACEHOLDER_ACCOUNT_ID.r2.cloudflarestorage.com" }
